# Swift / SwiftPM build output
.build/
*.o
*.swiftmodule

# App bundle + icon/dmg build intermediates
ela.app/
build/
*.dmg

# Large raw corpora (download via scripts/build_model.py)
data/raw/

# Generated debug model files (the binary model in data/model/*.bin is kept)
data/model/*.tsv
data/model/*.json
data/model/test.txt

# macOS
.DS_Store

# local Claude Code settings
.claude/
